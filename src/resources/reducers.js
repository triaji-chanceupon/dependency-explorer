import dependencies, { dependency } from '@resources/Dependency/reducers';

import packages, { packag } from '@resources/Package/reducers';

export const resourceReducers = { dependencies, dependency, packages, packag };
